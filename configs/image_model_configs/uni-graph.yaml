train_data:
  sample_id: Spatial_CITEseq_Tonsil1
  hires_image_key: CytAssist_FFPE_Protein_Expression_Human_Tonsil

test_data:
  sample_id: Spatial_CITEseq_Tonsil2
  hires_image_key: CytAssist_FFPE_Protein_Expression_Human_Tonsil_AddOns

image_model:
  hf_repo_id: 'uni'
  pretrained: true
  freeze_parameters: false
  use_predictions_as_features: false

model:
  graph_layer: 'GAT' # GCN, GAT
  gat_heads: 4
  fusion_method: 'gated' # concat, gated
  gene_hidden_dim: 256

hyperparameters:
  train_batch_size: 64
  test_batch_size: 64
  patch_size: 256
  epochs: 100
  learning_rate: 0.001
  target_num_genes: 2000
  random_seed: 42
  use_validation: false
  graph_k: 7

logging:
  use_wandb: true
  wandb_project: 'spatial_transcriptomics_hf'
  wandb_name: 'uni-GAT-7-gated'